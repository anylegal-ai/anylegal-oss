# AnyLegal OSS — backend Python deps.
#
# Pinning style: floor versions only (>=). The OSS distribution targets
# the broadest plausible installs; we leave upper bounds to whatever the
# current resolver picks at install time.
#
# Source-of-truth is requirements.in (manually curated). This file is
# expected to be the same content for the OSS release — we are not
# running pip-compile to expand into a fully-locked tree, since the OSS
# user is going to `pip install -r requirements.txt` against arbitrary
# Python versions. Keep the two files in sync when adding deps.

# ── HTTP / API framework ──────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart  # Required by FastAPI for File/Form (peer dep, not directly imported)

# ── Async / data ──────────────────────────────────────────────────
httpx>=0.25.0
pydantic
pyyaml>=6.0
python-dotenv
numpy

# ── Validation / security ─────────────────────────────────────────
defusedxml>=0.7.0
cryptography
bcrypt
markdown

# ── LLM API client ────────────────────────────────────────────────
openai

# ── Document I/O (DOCX / XLSX / PPTX / PDF) ──────────────────────
python-docx>=0.8.11
docx-revisions>=0.1.3   # Native Word track-changes
mammoth>=1.6.0          # DOCX → HTML
htmldocx                # Markdown → DOCX (used by create_document skill)
openpyxl>=3.1.0         # XLSX
python-pptx>=0.6.21     # PPTX
pymupdf4llm>=0.0.25     # PDF extraction
lxml

# ── Diff engine ───────────────────────────────────────────────────
diff-match-patch>=20230430

# ── HTTP fetch + search ───────────────────────────────────────────
requests
ddgs                    # DuckDuckGo search (rebrand of duckduckgo-search)

# ── LexWiki compiler ──────────────────────────────────────────────
# Workspace knowledge-base sidecar deps. Installed from GitHub; not on
# PyPI yet. Pinned tag — bump explicitly with new releases.
lexwiki @ git+https://github.com/wouldbe12/lexwiki@v0.1.0

# ── Runtime metrics / housekeeping ────────────────────────────────
psutil

# ── Testing ───────────────────────────────────────────────────────
pytest
pytest-asyncio>=0.23
